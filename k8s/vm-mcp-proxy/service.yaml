apiVersion: v1
kind: Service
metadata:
  name: vm-mcp-proxy
  namespace: observability
  labels:
    app: vm-mcp-proxy
    component: mcp-proxy
spec:
  type: ClusterIP
  selector:
    app: vm-mcp-proxy
  ports:
  - name: http
    port: 8084
    targetPort: 8084
    protocol: TCP
