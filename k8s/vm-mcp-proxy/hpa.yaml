apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: vm-mcp-proxy
  namespace: observability
  labels:
    app: vm-mcp-proxy
    component: mcp-proxy
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vm-mcp-proxy
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
