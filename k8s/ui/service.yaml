apiVersion: v1
kind: Service
metadata:
  name: copilot-ui
  namespace: copilot
  labels:
    app: copilot-ui
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: copilot-ui
  ports:
  - name: http
    port: 8501
    targetPort: 8501
    protocol: TCP
