apiVersion: v1
kind: Service
metadata:
  name: vm-mcp-python
  namespace: observability
  labels:
    app: vm-mcp-python
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: vm-mcp-python
  ports:
  - name: http
    port: 8085
    targetPort: 8085
    protocol: TCP
