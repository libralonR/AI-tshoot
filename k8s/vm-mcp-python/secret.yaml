apiVersion: v1
kind: Secret
metadata:
  name: vm-mcp-python-secrets
  namespace: observability
  labels:
    app: vm-mcp-python
    component: mcp-server
type: Opaque
data:
  # base64 encoded bearer token (optional)
  # echo -n "your-token" | base64
  vm-bearer-token: ""
