apiVersion: v1
kind: Service
metadata:
  name: incidents-pg-mcp-server
  namespace: observability
  labels:
    app: incidents-pg-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: incidents-pg-mcp-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
