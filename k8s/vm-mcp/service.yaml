apiVersion: v1
kind: Service
metadata:
  name: vm-mcp-server
  namespace: observability
  labels:
    app: vm-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  selector:
    app: vm-mcp-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
